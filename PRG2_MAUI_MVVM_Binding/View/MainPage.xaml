<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PRG2_MAUI_MVVM_Binding.MainPage">

    <ScrollView>
        <VerticalStackLayout Padding="10" Spacing="10">

            <Label Text="Studenter"
                   FontSize="28"
                   Margin="0,0,0,10" />

            <CollectionView ItemsSource="{Binding StudentList}"
                            SelectedItem="{Binding SelectedStudent, Mode=TwoWay}"
                            SelectionMode="Single">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <HorizontalStackLayout Spacing="20">
                            <Label Text="{Binding FirstName}" />
                            <Label Text="{Binding SecondName}" />
                        </HorizontalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Label Text="Markerad student"
                   FontSize="18"
                   Margin="0,20,0,0" />

            <Label Text="{Binding SelectedStudent.FirstName}" />
            <Label Text="{Binding SelectedStudent.Age}" />

            <Label Text="Ny student"
                   FontSize="18"
                   Margin="0,20,0,0" />

            <Entry Placeholder="Förnamn"
                   Text="{Binding NewFirstName}" />

            <Entry Placeholder="Efternamn"
                   Text="{Binding NewSecondName}" />

            <Entry Placeholder="Ålder"
                   Keyboard="Numeric"
                   Text="{Binding NewAge}" />

            <Button Text="Lägg till student"
                    Command="{Binding AddStudentCommand}" />

            <Button Text="Spara till fil"
                    Command="{Binding SaveCommand}" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
